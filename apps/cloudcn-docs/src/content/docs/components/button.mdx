---
title: Button
description: A multi-variant button for React and Svelte, built on Ark UI.
group: components
order: 1
---

The Button component triggers an action. It is implemented in both React and Svelte on top of Ark UI, sharing `buttonVariants` and the theme from `cloudcn-core`.

Full variant documentation and a live framework-switchable demo are added in a later milestone.
