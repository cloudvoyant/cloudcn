// libs/vortex-react/src/chat/icons.tsx
// Closely based on: Phosphor Icons (@phosphor-icons/react) — the repo's sourced
// icon package; aliases only, no hand-rolled SVG.
// Chat family icons via Phosphor Icons (@phosphor-icons/react) — tree-shaken,
// no hand-rolled SVG. Aliases keep the family's *Icon naming; Phosphor icons
// accept all SVG props (className passes through), default to 1em/currentColor.
// The icon prop type is Phosphor's own `IconProps` — consumers import it from
// `@phosphor-icons/react` rather than through a family-specific alias.
export {
  PaperclipIcon,
  PaperPlaneRightIcon as SendIcon,
  ThumbsDownIcon,
  ThumbsUpIcon,
  XIcon,
} from '@phosphor-icons/react';
