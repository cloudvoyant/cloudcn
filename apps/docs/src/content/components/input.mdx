---
title: Input
description: Styled text input for React and Svelte, built on Ark UI field.
order: 9
group: forms
---

import Demo from '../../components/Demo.astro';
import Examples from '../../components/Examples.astro';

The `Input` component is a styled native input that works standalone or inside a `Field`. It is built on [Ark UI `field`](https://ark-ui.com/docs/components/field), sharing the `inputVariants` cva from `@cloudvoyant/helix`.

<Demo component="input" example="default" />

## Examples

<Examples component="input" exclude="default" />

## API Reference

### Input

| Prop | Type | Default |
| --- | --- | --- |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` |
| `variant` | `'default' \| 'ghost'` | `'default'` |
| `type` | `string` | `'text'` |
| `placeholder` | `string` | — |
| `disabled` | `boolean` | `false` |
| `invalid` | `boolean` | `false` |
| `value` | `string` | — |
| `onChange` | `ChangeEventHandler<HTMLInputElement>` | — |
| `className` | `string` | — |

## Accessibility

Ark renders a native `<input>` with no extra role. Invalid state is announced via `aria-invalid` when inside a `Field`; pair with `FieldLabel`/`FieldError` for full labelling.

## Theming

No theme extension — uses `--input`, `--ring`, `--destructive`, `--muted-foreground` tokens, light and dark.
