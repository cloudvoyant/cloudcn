#!/usr/bin/env bash
#MISE description="Run Playwright E2E tests against the docs site (preview server)"
#MISE depends=["build"]
set -euo pipefail
cd "${MISE_PROJECT_ROOT:-$PWD}"
pnpm --filter cloudcn-docs run e2e
